#include "features/focus_reading/Registration.h"

namespace features::focus_reading {

void registerFeature() {
  // Capability registration happens via FeatureCatalog/FeatureModules;
  // no lifecycle hooks required for this feature.
}

}  // namespace features::focus_reading
