#!/usr/bin/env bash
# TEMPORARY: crossink port sprint — tracked fallback when core.hooksPath is .githooks.
set -euo pipefail
REPO_ROOT="$(git rev-parse --show-toplevel)"
exec "${REPO_ROOT}/scripts/hooks/pre-commit" "$@"
